pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/kanisma09/Django-project.git'
            }
        }

        stage('Build') {
            steps {
                sh 'python3 --version'
            }
        }
    }
}
